import { Manrope, Audiowide, Amatic_SC } from "next/font/google";

export const manrope = Manrope({
 subsets: ["latin"],
 display: "swap",
});

export const audiowide = Audiowide({
 subsets: ["latin"],
 weight: "400",
 display: "swap",
});

export const amatic_SC = Amatic_SC({
 subsets: ["latin"],
 weight: "400",
 display: "swap",
});
